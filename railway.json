{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install && pnpm build",
    "watchPatterns": ["src/**", "public/**"]
  },
  "deploy": {
    "static": true,
    "staticDir": "dist",
    "healthcheckPath": "/"
  }
}
